// ✅ Function: combine
// Accepts 3 inputs (a, b, c)
// Computes a * b / c
// Returns the result

function combine(a, b, c) {
  const result = (a * b) / c;
  return result;
}

// 🔍 Example usage
const output = combine(6, 4, 2); // (6 * 4) / 2 = 12
console.log(output); // 12
